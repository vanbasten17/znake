## ADDED Requirements

### Requirement: Scene transitions are routed through a dedicated state machine

The system SHALL support Scene transitions are routed through a dedicated state machine.

#### Scenario: Scene State Machine Split baseline contract
- **WHEN** this change is implemented and enabled
- **THEN** Scene transitions are routed through a dedicated state machine
- **AND** behavior remains deterministic and testable
