## ADDED Requirements

### Requirement: Interactive CSS classes reuse shared touch-target token

The system SHALL enforce this contract as part of the znake-ui-css-touch-target-token-propagation-v1 scope.

#### Scenario: Contract is applied
- **WHEN** the relevant runtime or UI path executes
- **THEN** the defined contract behavior is applied consistently
- **AND** deterministic simulation behavior remains unchanged.
