## ADDED Requirements

### Requirement: Upgrade effects are resolved via strategy registry

The system SHALL support Upgrade effects are resolved via strategy registry.

#### Scenario: Upgrade Strategy Registry baseline contract
- **WHEN** this change is implemented and enabled
- **THEN** Upgrade effects are resolved via strategy registry
- **AND** behavior remains deterministic and testable
