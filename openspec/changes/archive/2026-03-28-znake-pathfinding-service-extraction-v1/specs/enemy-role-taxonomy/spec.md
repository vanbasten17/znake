## ADDED Requirements

### Requirement: Enemy pathfinding uses strategy service interfaces

The system SHALL support Enemy pathfinding uses strategy service interfaces.

#### Scenario: Pathfinding Service Extraction baseline contract
- **WHEN** this change is implemented and enabled
- **THEN** Enemy pathfinding uses strategy service interfaces
- **AND** behavior remains deterministic and testable
