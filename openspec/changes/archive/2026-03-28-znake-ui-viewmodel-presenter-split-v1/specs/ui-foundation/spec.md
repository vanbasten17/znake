## ADDED Requirements

### Requirement: UI rendering consumes dedicated view-model presenters

The system SHALL support UI rendering consumes dedicated view-model presenters.

#### Scenario: UI ViewModel Presenter Split baseline contract
- **WHEN** this change is implemented and enabled
- **THEN** UI rendering consumes dedicated view-model presenters
- **AND** behavior remains deterministic and testable
