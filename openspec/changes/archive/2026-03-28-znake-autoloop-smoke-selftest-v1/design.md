## Context

This is a controlled self-test change intended to validate autoloop execution on a real but tiny code modification. The change is tooling-only and keeps simulation/rendering untouched.

## Key Points (Codex-style)

- **What is changing**
  - Add `loopVersion` in loop report JSON.
- **Why we are doing it**
  - Prove the autonomous cycle works with a low-risk live delta.
- **Impacted areas**
  - Report artifact schema generated by `tools/autoloop.mjs`.
- **Risks / unknowns**
  - No functional risk expected.

## Goals / Non-Goals

**Goals:**
- Add deterministic report version metadata.
- Execute autoloop for this specific change and verify success artifacts.

**Non-Goals:**
- Any gameplay logic changes.
- Any archive automation changes.

## Decisions

- Keep the field static (`loopVersion: "1.0.1"`) for this self-test iteration.
- Keep the implementation localized to the JSON report object only.

## Risks / Trade-offs

- [Future schema drift] -> Mitigation: version marker helps downstream parsers detect shape changes.
