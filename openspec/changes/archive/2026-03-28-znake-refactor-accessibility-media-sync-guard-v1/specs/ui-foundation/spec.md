## ADDED Requirements

### Requirement: Accessibility media sync avoids duplicate listeners

The system SHALL enforce this contract as part of the znake-refactor-accessibility-media-sync-guard-v1 scope.

#### Scenario: Contract is applied
- **WHEN** the relevant runtime or UI path executes
- **THEN** the defined contract behavior is applied consistently
- **AND** deterministic simulation behavior remains unchanged.
