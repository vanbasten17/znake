## ADDED Requirements

### Requirement: Visual accessibility presets apply across menu and gameplay surfaces

The system SHALL support Visual accessibility presets apply across menu and gameplay surfaces.

#### Scenario: Visual Accessibility Presets baseline contract
- **WHEN** this change is implemented and enabled
- **THEN** Visual accessibility presets apply across menu and gameplay surfaces
- **AND** behavior remains deterministic and testable
