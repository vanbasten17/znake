## ADDED Requirements

### Requirement: Input commands are validated before simulation mutation

The system SHALL support Input commands are validated before simulation mutation.

#### Scenario: Input Command Pipeline baseline contract
- **WHEN** this change is implemented and enabled
- **THEN** Input commands are validated before simulation mutation
- **AND** behavior remains deterministic and testable
