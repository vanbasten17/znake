## ADDED Requirements

### Requirement: HUD hierarchy prioritizes critical status over secondary info

The system SHALL support HUD hierarchy prioritizes critical status over secondary info.

#### Scenario: HUD Visual Hierarchy Pass baseline contract
- **WHEN** this change is implemented and enabled
- **THEN** HUD hierarchy prioritizes critical status over secondary info
- **AND** behavior remains deterministic and testable
