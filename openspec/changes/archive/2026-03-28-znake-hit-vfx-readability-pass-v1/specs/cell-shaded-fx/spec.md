## ADDED Requirements

### Requirement: Combat feedback VFX uses distinct semantic channels

The system SHALL support Combat feedback VFX uses distinct semantic channels.

#### Scenario: Hit VFX Readability Pass baseline contract
- **WHEN** this change is implemented and enabled
- **THEN** Combat feedback VFX uses distinct semantic channels
- **AND** behavior remains deterministic and testable
