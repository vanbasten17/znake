## ADDED Requirements

### Requirement: Content consumers depend on repository interfaces, not concrete loaders

The system SHALL support Content consumers depend on repository interfaces, not concrete loaders.

#### Scenario: Content Repository Ports baseline contract
- **WHEN** this change is implemented and enabled
- **THEN** Content consumers depend on repository interfaces, not concrete loaders
- **AND** behavior remains deterministic and testable
