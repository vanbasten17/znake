# Neon marker proposals

Neon-style proposals for non-enemy, non-talent marker tones (excluding core).

Generated outputs:
- SVG source: this folder
- PNG preview/export: `./png` (via `pnpm sprites:svg2png`)

